.DS_Store
*.bak
*.qmlc

# Local coding-agent instructions. Deliberately not distributed: the
# marketplace treats auto-consumed agent instructions as out of scope for a
# plugin, and the file documents destructive dev-loop commands.
CLAUDE.md
